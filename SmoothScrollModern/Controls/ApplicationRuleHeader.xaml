<?xml version="1.0" encoding="utf-8" ?>
<UserControl
    x:Class="SmoothScrollModern.Controls.ApplicationRuleHeader"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid ColumnSpacing="14">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" MinWidth="260" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <StackPanel VerticalAlignment="Center" Spacing="4">
            <TextBlock
                FontSize="13"
                FontWeight="SemiBold"
                Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"
                Text="{x:Bind Label, Mode=OneWay}"
                Visibility="{x:Bind HasLabel, Mode=OneWay}" />
            <TextBlock
                FontSize="16"
                FontWeight="SemiBold"
                Text="{x:Bind Title, Mode=OneWay}"
                TextTrimming="CharacterEllipsis" />
            <TextBlock
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                Text="{x:Bind Subtitle, Mode=OneWay}"
                TextTrimming="CharacterEllipsis"
                TextWrapping="Wrap"
                Visibility="{x:Bind HasSubtitle, Mode=OneWay}" />
            <TextBlock
                Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                Text="{x:Bind Detail, Mode=OneWay}"
                TextTrimming="CharacterEllipsis"
                Visibility="{x:Bind HasDetail, Mode=OneWay}" />
        </StackPanel>

        <ContentControl
            Grid.Column="1"
            HorizontalAlignment="Right"
            VerticalAlignment="Center"
            Content="{x:Bind ActionContent, Mode=OneWay}" />
    </Grid>
</UserControl>
